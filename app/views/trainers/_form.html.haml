= semantic_form_for @trainer do |f|
  - f.inputs :trainer_details do
    .grid_12
      = f.input :name, :input_html => { :size => 40 }
      = f.input :bio
      = f.actions
      = link_to t('formtastic.actions.cancel'), trainers_path
