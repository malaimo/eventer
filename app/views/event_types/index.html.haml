%h1= t('title.event_type.index')

%table
  %tr
    %th= t('formtastic.labels.event_type.name')
    %th= t('formtastic.labels.event_type.description')
    %th
    %th
    %th

  - @event_types.each do |event_type|
    %tr
      %td= event_type.name
      %td= event_type.description
      %td= link_to 'Show', event_type
      %td= link_to 'Edit', edit_event_type_path(event_type)
      %td= link_to 'Destroy', event_type, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Event type', new_event_type_path
