%ul#menu
  = link_to_menu_item(:li, t('menu.dashboard'), dashboard_path)
  = link_to_menu_item(:li, t('menu.roles'), roles_path)
  = link_to_menu_item(:li, t('menu.users'), users_path)
  - if can? :manage, Event.new
    = link_to_menu_item(:li, t('menu.events'), events_path)
  - if can? :manage, Trainer.new
    = link_to_menu_item(:li, t('menu.trainers'), trainers_path)
  = link_to_menu_item(:li, t('menu.exit'), destroy_user_session_path, :method => :delete)